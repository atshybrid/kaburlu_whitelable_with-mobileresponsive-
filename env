# Multitenancy
# NOTE: for production, keep this stable; for local testing override via .env.local
NEXT_PUBLIC_DEFAULT_TENANT="localhost"
MULTITENANT_MODE="path" # allowed: path | subdomain
# BASE_DOMAIN is not used by the app logic; API_BASE_URL is used below
# BASE_DOMAIN="your-base-domain.example.com"

# Data source and remote API
DATA_SOURCE="remote" # remote | local
API_BASE_URL="https://app.kaburlumedia.com/api/v1"
HOST="kaburlutoday.com"
NEXT_PUBLIC_HOST="kaburlutoday.com"

# Site metadata defaults
SITE_NAME="Kaburlu News"
SITE_DESCRIPTION="Multi-tenant news platform with multiple themes."
