# Copy to .env.local and set your domain before deploy
# Used for building absolute URLs (sharing, Open Graph/Twitter cards)
NEXT_PUBLIC_SITE_URL=https://your-domain.com

# Enable PWA build explicitly (optional)
# Set to true only when you want service worker + offline support built.
# ENABLE_PWA=true
